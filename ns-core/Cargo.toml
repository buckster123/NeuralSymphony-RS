[package]
name = "ns-core"
description = "NeuralSymphony mapping core: memory graph → musical IR. Pure, deterministic, no I/O."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
